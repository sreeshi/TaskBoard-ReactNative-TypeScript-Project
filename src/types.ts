// src/types.ts

export interface Task {
  id: number;
  text: string;
  priority: number;
}


/*export interface Task {
  id: number;
  text: string;
  completed?: boolean;  // optional, for future use
}*/
